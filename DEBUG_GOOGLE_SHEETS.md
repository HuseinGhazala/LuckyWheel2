# 🔍 حل مشكلة عدم وصول البيانات إلى Google Sheets

## المشكلة:
البيانات لا تصل إلى Google Sheets

## الحلول:

### ✅ الحل 1: تحقق من رابط Google Script

1. افتح التطبيق → Settings → admin
2. تحقق من حقل "رابط نشر التطبيق"
3. يجب أن يكون الرابط مثل:
   ```
   https://script.google.com/macros/s/AKfycb.../exec
   ```
4. إذا كان فارغاً أو خاطئاً، أدخل الرابط الصحيح واحفظ

### ✅ الحل 2: افتح Console للتحقق

1. افتح التطبيق في المتصفح
2. اضغط **F12** → **Console**
3. ابحث عن رسائل:
   - "🔗 الرابط: ..." - يجب أن يظهر رابط Google Script
   - "💾 جاري حفظ البيانات في Google Sheets..."
   - "✅ تم إرسال البيانات إلى Google Sheets"

### ✅ الحل 3: تحقق من Google Apps Script

1. افتح Google Apps Script
2. اضغط **View** → **Execution log**
3. ابحث عن أي أخطاء
4. تأكد من أن الكود من `google-apps-script.js` موجود

### ✅ الحل 4: تحقق من نشر التطبيق

1. في Google Apps Script، اضغط **Deploy** → **Manage deployments**
2. تأكد من:
   - **Execute as**: "أنا (your-email@gmail.com)"
   - **Who has access**: "أي شخص" (Anyone)
3. إذا لم يكن كذلك، عدّل ونشر مرة أخرى

### ✅ الحل 5: اختبر الرابط مباشرة

افتح الرابط في المتصفح:
```
https://script.google.com/macros/s/YOUR_ID/exec?action=getSettings
```

يجب أن ترى JSON response.

---

## 🔍 خطوات التشخيص:

### 1. افتح Console (F12)
ابحث عن:
- ⚠️ "رابط Google Script غير محدد"
- ❌ أي رسائل خطأ

### 2. تحقق من localStorage
في Console، اكتب:
```javascript
localStorage.getItem('googleScriptUrl')
```
يجب أن ترى رابط Google Script

### 3. تحقق من Google Sheet
1. افتح Google Sheet
2. تحقق من وجود الأوراق: Settings, UserData, Wins
3. إذا لم تكن موجودة، شغّل دالة `createAllSheets` في Apps Script

---

## ⚠️ مشاكل شائعة:

### المشكلة: "رابط Google Script غير محدد"
**الحل:**
- أدخل الرابط في لوحة التحكم
- احفظ التغييرات
- حدّث الصفحة

### المشكلة: البيانات لا تظهر في Google Sheet
**الحل:**
- تأكد من نشر التطبيق بشكل صحيح
- تأكد من أن "Who has access" = "Anyone"
- تحقق من Execution log في Apps Script

### المشكلة: CORS error
**الحل:**
- تأكد من نشر التطبيق كـ Web app
- تأكد من الإعدادات الصحيحة

---

## ✅ اختبار سريع:

1. افتح Console (F12)
2. احفظ إعدادات من لوحة التحكم
3. ابحث عن رسائل:
   - "💾 جاري حفظ البيانات في Google Sheets..."
   - "✅ تم إرسال البيانات إلى Google Sheets"
4. ارجع إلى Google Sheet وتحقق من البيانات

---

## 📞 إذا لم يعمل:

أرسل لي:
1. رسائل Console (F12)
2. رابط Google Script الذي تستخدمه
3. أي أخطاء من Execution log في Apps Script
