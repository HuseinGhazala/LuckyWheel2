# 📚 توثيق شامل لمشروع عجلة الحظ (Lucky Wheel)

## 📋 جدول المحتويات
1. [نظرة عامة](#نظرة-عامة)
2. [المميزات الرئيسية](#المميزات-الرئيسية)
3. [المتطلبات والتقنيات المستخدمة](#المتطلبات-والتقنيات-المستخدمة)
4. [إعداد المشروع](#إعداد-المشروع)
5. [إعداد Supabase](#إعداد-supabase)
6. [إعداد Google Sheets](#إعداد-google-sheets)
7. [هيكل المشروع](#هيكل-المشروع)
8. [الوظائف الرئيسية](#الوظائف-الرئيسية)
9. [كيفية الاستخدام](#كيفية-الاستخدام)
10. [نشر المشروع](#نشر-المشروع)

---

## 🎯 نظرة عامة

**عجلة الحظ** هو تطبيق ويب تفاعلي يسمح للمستخدمين بالدوران على عجلة الحظ والفوز بجوائز حقيقية (كوبونات خصم). التطبيق مبني باستخدام React.js ويدعم التخزين السحابي عبر Supabase و Google Sheets.

### الهدف من المشروع
- إنشاء نظام عجلة حظ تفاعلي وجذاب
- إدارة الجوائز والكوبونات بشكل مركزي
- جمع بيانات المستخدمين والجوائز الفائزة
- توفير لوحة تحكم شاملة لإدارة الإعدادات

---

## ✨ المميزات الرئيسية

### 1. عجلة الحظ التفاعلية
- تصميم جذاب ومتحرك
- نظام احتمالات مرجح للتحكم في ظهور الجوائز
- تأثيرات بصرية وصوتية عند الفوز
- دعم RTL (من اليمين لليسار) للغة العربية

### 2. إدارة الجوائز والكوبونات
- إضافة/تعديل/حذف الجوائز
- إدارة الكوبونات لكل جائزة
- نظام منع تكرار الكوبونات المستخدمة
- التحكم في نسبة ظهور كل جائزة

### 3. لوحة التحكم (Dashboard)
- إدارة جميع الإعدادات من مكان واحد
- رفع شعار المتجر
- إدارة روابط التواصل الاجتماعي
- تخصيص الخلفيات (لون أو صورة)
- إدارة الأصوات (الفوز/الخسارة)
- إعدادات Google Script URL

### 4. التخزين السحابي
- **Supabase**: قاعدة بيانات PostgreSQL سحابية
- **Google Sheets**: تخزين البيانات في جداول Excel
- مزامنة تلقائية بين السحابة والمتصفح
- نسخة احتياطية محلية (localStorage)

### 5. جمع البيانات
- تسجيل بيانات المستخدمين (الاسم، البريد، الهاتف)
- حفظ الجوائز الفائزة مع الكوبونات
- تتبع جميع المحاولات والتاريخ

### 6. الأمان والحماية
- حماية من فتح Developer Tools
- منع نسخ النص واختياره
- منع النقر بالزر الأيمن
- كود دولة تلقائي للهواتف السعودية (+966)

---

## 🛠 المتطلبات والتقنيات المستخدمة

### التقنيات الأساسية
- **React.js 18.2.0** - مكتبة JavaScript لبناء واجهات المستخدم
- **Vite 4.4.5** - أداة بناء سريعة
- **Tailwind CSS** - إطار عمل CSS للتصميم
- **Lucide React** - مكتبة الأيقونات

### قواعد البيانات السحابية
- **Supabase** - قاعدة بيانات PostgreSQL + API
- **Google Sheets** - جداول البيانات عبر Google Apps Script

### المكتبات الإضافية
- **@supabase/supabase-js** - عميل Supabase
- **IBM Plex Sans Arabic** - خط عربي احترافي

---

## 🚀 إعداد المشروع

### 1. تثبيت المتطلبات
```bash
# تثبيت Node.js (الإصدار 16 أو أحدث)
# تحميل من: https://nodejs.org/

# تثبيت Git (اختياري)
# تحميل من: https://git-scm.com/
```

### 2. استنساخ المشروع
```bash
git clone <repository-url>
cd LuckyWheel
```

### 3. تثبيت المكتبات
```bash
npm install
```

### 4. تشغيل المشروع
```bash
npm run dev
```

المشروع سيعمل على: `http://localhost:5173`

### 5. بناء المشروع للإنتاج
```bash
npm run build
```

الملفات المبنية ستكون في مجلد `dist/`

---

## 🗄 إعداد Supabase

### الخطوة 1: إنشاء حساب Supabase
1. اذهب إلى [supabase.com](https://supabase.com)
2. سجل حساب جديد (مجاني)
3. أنشئ مشروع جديد

### الخطوة 2: الحصول على المفاتيح
1. في لوحة التحكم، اذهب إلى **Settings** → **API**
2. انسخ:
   - **Project URL** (مثل: `https://xxxxx.supabase.co`)
   - **anon/public key** (مفتاح طويل)

### الخطوة 3: إنشاء الجداول
1. اذهب إلى **SQL Editor** في Supabase
2. افتح ملف `supabase-schema.sql` من المشروع
3. انسخ الكود والصقه في SQL Editor
4. اضغط **Run** لتنفيذ الكود

### الجداول التي سيتم إنشاؤها:
- **settings**: تخزين إعدادات العجلة
- **user_data**: بيانات المستخدمين المسجلين
- **wins**: الجوائز الفائزة مع الكوبونات

### الخطوة 4: إعداد متغيرات البيئة
1. أنشئ ملف `.env` في جذر المشروع
2. أضف المفاتيح:
```env
VITE_SUPABASE_URL=https://xxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here
```

### الخطوة 5: التحقق من الإعداد
- شغّل المشروع: `npm run dev`
- افتح لوحة التحكم في التطبيق
- احفظ إعدادات جديدة
- تحقق من Supabase Dashboard → Table Editor → settings

---

## 📊 إعداد Google Sheets

### الخطوة 1: إنشاء Google Sheet
1. اذهب إلى [Google Sheets](https://sheets.google.com)
2. أنشئ جدول جديد
3. احفظه باسم مناسب (مثل: "Lucky Wheel Data")

### الخطوة 2: إنشاء Google Apps Script
1. في Google Sheet، اضغط **Extensions** → **Apps Script**
2. احذف الكود الافتراضي
3. افتح ملف `google-apps-script.js` من المشروع
4. انسخ كل الكود والصقه في Apps Script
5. احفظ الملف (Ctrl+S)

### الخطوة 3: إنشاء الأوراق المطلوبة
1. في Apps Script Editor، اضغط **Run** → **createAllSheets**
2. اختر حساب Google الخاص بك
3. وافق على الصلاحيات
4. انتظر حتى تظهر رسالة "تم إنشاء جميع الأوراق بنجاح!"

### الأوراق التي سيتم إنشاؤها:
- **Settings**: إعدادات العجلة
- **UserData**: بيانات المستخدمين
- **Wins**: الجوائز الفائزة

### الخطوة 4: نشر التطبيق
1. في Apps Script، اضغط **Deploy** → **New deployment**
2. اختر **Web app** (أيقونة الويب)
3. الإعدادات:
   - **Execute as**: Me
   - **Who has access**: Anyone
4. اضغط **Deploy**
5. انسخ **Web app URL** (مثل: `https://script.google.com/macros/s/xxxxx/exec`)

### الخطوة 5: إضافة الرابط في التطبيق
1. شغّل المشروع
2. افتح لوحة التحكم (أيقونة الإعدادات)
3. أدخل رابط Google Script في الحقل المخصص
4. احفظ الإعدادات

### الخطوة 6: التحقق من الإعداد
- سجّل بيانات مستخدم جديد
- تحقق من Google Sheet → ورقة **UserData**
- يجب أن تظهر البيانات الجديدة

---

## 📁 هيكل المشروع

```
LuckyWheel/
│
├── src/                          # كود المصدر
│   ├── App.jsx                  # المكون الرئيسي للتطبيق
│   ├── main.jsx                 # نقطة الدخول
│   └── lib/
│       └── supabase.js          # دوال Supabase
│
├── google-apps-script.js        # كود Google Apps Script
├── supabase-schema.sql          # مخطط قاعدة البيانات
│
├── index.html                   # الصفحة الرئيسية
├── package.json                 # إعدادات المشروع
├── vite.config.js              # إعدادات Vite
│
├── .env                         # متغيرات البيئة (غير موجود في Git)
├── .env.example                 # مثال على متغيرات البيئة
├── .gitignore                   # ملفات Git المستثناة
│
└── README.md                    # ملف README الأساسي
```

---

## 🔧 الوظائف الرئيسية

### 1. إدارة الجوائز (Segments)
```javascript
// إضافة جائزة جديدة
- النقر على "إضافة جائزة جديدة"
- إدخال النص واللون والنوع
- تحديد نسبة الظهور (weight)

// إدارة الكوبونات
- النقر على أيقونة التذكرة
- إدخال الكوبونات (سطر لكل كوبون)
- حفظ الكوبونات
```

### 2. نظام الكوبونات
- الكوبونات تُحذف تلقائياً بعد الاستخدام
- يتم حفظ التحديثات في السحابة فوراً
- الكوبونات المستخدمة لا تظهر مرة أخرى

### 3. نظام الاحتمالات المرجح
- كل جائزة لها وزن (weight)
- الأوزان الأعلى = احتمالية أعلى للظهور
- المجموع الكلي للأوزان يحدد الاحتمالية

### 4. التخزين السحابي
- **Supabase**: أولوية التحميل والحفظ
- **Google Sheets**: نسخة احتياطية
- **localStorage**: نسخة محلية للسرعة

### 5. جمع البيانات
- **UserData**: الاسم، البريد، الهاتف، التاريخ
- **Wins**: الاسم، البريد، الهاتف، الجائزة، الكوبون، التاريخ

---

## 📖 كيفية الاستخدام

### للمستخدمين العاديين:

1. **فتح التطبيق**
   - افتح الرابط في المتصفح
   - انتظر تحميل الإعدادات

2. **التسجيل**
   - اضغط "دور العجلة"
   - أدخل الاسم والبريد الإلكتروني
   - أدخل رقم الهاتف (سيتم إضافة +966 تلقائياً)
   - اضغط "سجل والعب الآن"

3. **الدوران**
   - اضغط على زر الدوران في وسط العجلة
   - انتظر حتى تتوقف العجلة
   - شاهد الجائزة الفائزة

4. **نسخ الكوبون**
   - إذا فزت بجائزة، اضغط على الكوبون لنسخه
   - استخدم الكوبون في المتجر

### للمطورين/المسؤولين:

1. **فتح لوحة التحكم**
   - اضغط على أيقونة الإعدادات (أعلى اليسار)
   - أدخل كلمة المرور (افتراضي: `admin`)

2. **إدارة الجوائز**
   - أضف/عدّل/احذف الجوائز
   - حدد نسبة الظهور لكل جائزة
   - أضف كوبونات لكل جائزة

3. **تخصيص التصميم**
   - ارفع شعار المتجر
   - أضف روابط التواصل الاجتماعي
   - اختر خلفية (لون أو صورة)
   - غيّر الأصوات

4. **إعدادات السحابة**
   - أدخل رابط Google Script
   - تأكد من إعدادات Supabase في `.env`
   - احفظ الإعدادات

5. **مشاهدة البيانات**
   - **Supabase**: Dashboard → Table Editor
   - **Google Sheets**: افتح الجدول → الأوراق

---

## 🌐 نشر المشروع

### الخيار 1: Vercel (موصى به)
```bash
# تثبيت Vercel CLI
npm i -g vercel

# النشر
vercel

# أو عبر الموقع
# 1. اذهب إلى vercel.com
# 2. سجل دخول بـ GitHub
# 3. استورد المشروع
# 4. أضف متغيرات البيئة (.env)
# 5. انشر
```

### الخيار 2: Netlify
```bash
# تثبيت Netlify CLI
npm i -g netlify-cli

# بناء المشروع
npm run build

# النشر
netlify deploy --prod
```

### الخيار 3: GitHub Pages
```bash
# بناء المشروع
npm run build

# نشر dist/ إلى GitHub Pages
# اتبع إرشادات GitHub Pages
```

### متغيرات البيئة المطلوبة للنشر:
```env
VITE_SUPABASE_URL=your-supabase-url
VITE_SUPABASE_ANON_KEY=your-supabase-key
```

---

## 🔐 الأمان

### الحماية المطبقة:
- ✅ منع فتح Developer Tools (F12, Ctrl+Shift+I)
- ✅ منع النقر بالزر الأيمن
- ✅ منع نسخ واختيار النص
- ✅ كشف محاولات فتح Console

### ملاحظات أمنية:
- ⚠️ كلمة مرور لوحة التحكم افتراضية (`admin`) - غيّرها في الكود
- ⚠️ مفتاح Supabase Anon Key آمن للاستخدام في Frontend
- ⚠️ Google Script URL يجب أن يكون "Anyone" للوصول العام

---

## 🐛 استكشاف الأخطاء

### المشكلة: البيانات لا تُحفظ في Google Sheets
**الحل:**
1. تحقق من رابط Google Script
2. تأكد من أن التطبيق منشور كـ "Anyone"
3. تحقق من Execution Log في Apps Script
4. تأكد من وجود الأوراق الثلاثة (Settings, UserData, Wins)

### المشكلة: البيانات لا تُحفظ في Supabase
**الحل:**
1. تحقق من `.env` يحتوي على المفاتيح الصحيحة
2. تأكد من إنشاء الجداول في Supabase
3. تحقق من Console للأخطاء
4. تأكد من أن RLS (Row Level Security) معطل أو مُعد بشكل صحيح

### المشكلة: الكوبونات تظهر مرة أخرى بعد الاستخدام
**الحل:**
1. تأكد من حفظ الإعدادات بعد استخدام الكوبون
2. تحقق من Console لرسائل الحفظ
3. تأكد من اتصال الإنترنت

### المشكلة: الخط العربي لا يظهر بشكل صحيح
**الحل:**
1. تأكد من اتصال الإنترنت (الخط من Google Fonts)
2. تحقق من أن `index.html` يحتوي على رابط الخط
3. امسح Cache المتصفح

---

## 📝 ملاحظات مهمة

### 1. الكوبونات
- الكوبونات تُحذف تلقائياً بعد الاستخدام
- يتم حفظ التحديثات في السحابة فوراً
- الكوبونات المستخدمة لا تُعاد حتى بعد إعادة تعيين اللعبة

### 2. البيانات السحابية
- Supabase له أولوية في التحميل والحفظ
- Google Sheets يعمل كنسخة احتياطية
- localStorage يستخدم كنسخة محلية للسرعة

### 3. رقم الهاتف
- يتم إضافة كود الدولة السعودية (+966) تلقائياً
- الرقم يجب أن يبدأ بـ 05 (مثل: 0501234567)
- يتم حفظه كـ +966501234567

### 4. الأصوات
- يمكن رفع ملفات صوتية مخصصة
- أو استخدام روابط من الإنترنت
- الأصوات الافتراضية من soundjay.com

---

## 📞 الدعم والمساعدة

### للمشاكل التقنية:
1. تحقق من Console في المتصفح (F12)
2. تحقق من Execution Log في Google Apps Script
3. تحقق من Logs في Supabase Dashboard

### للمساعدة في الإعداد:
- راجع قسم "إعداد المشروع" أعلاه
- راجع قسم "استكشاف الأخطاء"

---

## 📄 الترخيص

هذا المشروع مفتوح المصدر ويمكن استخدامه بحرية.

---

## 🎉 شكراً لاستخدامك عجلة الحظ!

تم تطوير هذا المشروع بعناية ليكون أداة قوية وسهلة الاستخدام لإدارة عجلة الحظ والجوائز.

**تم التحديث:** فبراير 2026
**الإصدار:** 1.0.0
